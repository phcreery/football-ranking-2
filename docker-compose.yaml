version: '3.3'

services:
  app:
    build:
      context: ./client/
      dockerfile: ./Dockerfile
    ports:
      - "127.0.0.1:8000:8000"

  server:
    env_file:
      - server/.env
    build:
      context: ./server/
      dockerfile: ./Dockerfile
    ports:
      - "127.0.0.1:8001:8001"